networks:
  elastic:
    name: ds_elastic_production
    driver: bridge

volumes:
  elasticsearch_data:
    name: ds_elasticsearch_data_production
  kibana_data:
    name: ds_kibana_data_production
  filebeat_data:
    name: ds_filebeat_data_production

services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.13.4
    container_name: elasticsearch-production
    platform: linux/amd64
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false # TODO: add pw for security purposes
      # - ELASTIC_PASSWORD=<password>
    networks:
      - elastic
    ports:
      - "9201:9200"
      - "9301:9300"
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
    env_file:
      - .env

  kibana:
    image: docker.elastic.co/kibana/kibana:8.13.4
    container_name: kibana-production
    platform: linux/amd64
    environment:
      - ELASTICSEARCH_HOSTS=["http://elasticsearch-production:9200"]
    networks:
      - elastic
    ports:
      - "5602:5601"
    volumes:
      - kibana_data:/usr/share/kibana/data
    depends_on:
      - elasticsearch
    env_file:
      - .env

  filebeat:
    image: docker.elastic.co/beats/filebeat:8.12.0
    user: root
    container_name: filebeat-production
    platform: linux/amd64
    networks:
      - elastic
    depends_on:
      - elasticsearch
    volumes:
      - filebeat_data:/usr/share/filebeat/data
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./logs:/app/logs:ro
      - ./filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - ./filebeat/filebeat-5d-policy.json:/usr/share/filebeat/filebeat-5d-policy.json:ro
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch-production:9200
    env_file:
      - .env

  tap-yfinance:
    build:
      context: ./projects/financial-elt/
      dockerfile: Dockerfile
      args:
        CI: "true"
    container_name: tap-yfinance-production
    platform: linux/amd64
    networks:
      - elastic
    env_file:
      - .env
    environment:
      - FINANCIAL_ELT_TAPS_TO_RUN=tap-yfinance
    volumes:
      - ./logs:/app/logs
      - ./projects/financial-elt/tap-yfinance/output:/app/tap-yfinance/output
      - ./projects/financial-elt/tap-yfinance/state:/app/tap-yfinance/state
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - "5010:5000"

  tap-yahooquery:
    build:
      context: ./projects/financial-elt/
      dockerfile: Dockerfile
      args:
        CI: "true"
    container_name: tap-yahooquery-production
    platform: linux/amd64
    networks:
      - elastic
    env_file:
      - .env
    environment:
      - FINANCIAL_ELT_TAPS_TO_RUN=tap-yahooquery
    volumes:
      - ./logs:/app/logs
      - ./projects/financial-elt/tap-yahooquery/output:/app/tap-yahooquery/output
      - ./projects/financial-elt/tap-yahooquery/state:/app/tap-yahooquery/state
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - "5011:5000"

  tap-polygon:
    build:
      context: ./projects/financial-elt/
      dockerfile: Dockerfile
      args:
        CI: "true"
    container_name: tap-polygon-production
    platform: linux/amd64
    networks:
      - elastic
    env_file:
      - .env
    environment:
      - FINANCIAL_ELT_TAPS_TO_RUN=tap-polygon
    volumes:
      - ./logs:/app/logs
      - ./projects/financial-elt/tap-polygon/output:/app/tap-polygon/output
      - ./projects/financial-elt/tap-polygon/state:/app/tap-polygon/state
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - "5012:5000"

  tap-fmp:
    build:
      context: ./projects/financial-elt/
      dockerfile: Dockerfile
      args:
        CI: "true"
    container_name: tap-fmp-production
    platform: linux/amd64
    networks:
      - elastic
    env_file:
      - .env
    environment:
      - FINANCIAL_ELT_TAPS_TO_RUN=tap-fmp
    volumes:
      - ./logs:/app/logs
      - ./projects/financial-elt/tap-fmp/output:/app/tap-fmp/output
      - ./projects/financial-elt/tap-fmp/state:/app/tap-fmp/state
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - "5013:5000"

  tap-fred:
    build:
      context: ./projects/financial-elt/
      dockerfile: Dockerfile
      args:
        CI: "true"
    container_name: tap-fred-production
    platform: linux/amd64
    networks:
      - elastic
    env_file:
      - .env
    environment:
      - FINANCIAL_ELT_TAPS_TO_RUN=tap-fred
    volumes:
      - ./logs:/app/logs
      - ./projects/financial-elt/tap-fred/output:/app/tap-fred/output
      - ./projects/financial-elt/tap-fred/state:/app/tap-fred/state
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    ports:
      - "5014:5000"